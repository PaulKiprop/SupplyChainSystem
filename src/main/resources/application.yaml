spring:
  datasource:
    url: jdbc:mysql://localhost:3306/supply_chain_db
    username: user
    password: root
  jpa:
    hibernate:
      ddl-auto: update   # use 'create' only for fresh start
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
