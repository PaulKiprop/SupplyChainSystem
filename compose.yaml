version: '3.8'

services:
  mysql:
    image: mysql:8.4
    container_name: supply_mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: supply_chain_db
    ports:
      - "3306:3306"
